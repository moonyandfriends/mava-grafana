# Grafana Configuration
# The URL where Grafana is accessible (default: http://localhost:3000)
GRAFANA_URL=http://localhost:3000

# Grafana Admin Credentials
# Default admin user credentials (change these for production!)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Supabase Database Connection
# Replace with your actual Supabase connection details
SUPABASE_HOST=db.your-project-ref.supabase.co
SUPABASE_DB=postgres
SUPABASE_USER=postgres
SUPABASE_PASSWORD=your-database-password
SUPABASE_PORT=5432

# Grafana Viewer User (optional)
# Creates a read-only viewer user with access to all dashboards
GRAFANA_VIEWER_USER=viewer
GRAFANA_VIEWER_PASSWORD=viewer_password
GRAFANA_VIEWER_EMAIL=viewer@example.com

# Ambassador User Credentials (optional)
# Creates a user with access ONLY to the ambassador dashboard
# If these are not set, the ambassador user will not be created
GRAFANA_AMBASSADOR_USER=ambassador
GRAFANA_AMBASSADOR_PASSWORD=ambassador_password
GRAFANA_AMBASSADOR_EMAIL=ambassador@example.com

# Note: Ambassador users will:
# - Only see the ambassador dashboard
# - Be automatically added to the "Ambassadors" team
# - Have view-only permissions to the ambassador dashboard
# - Cannot access any other dashboards or admin functions